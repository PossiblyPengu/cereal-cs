<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="using:Cereal.App"
             xmlns:local="using:Cereal.App.Views.Panels"
             x:Class="Cereal.App.Views.Panels.ChiakiPanel"
             x:DataType="local:ChiakiPanelViewModel"
             Background="Transparent">

  <UserControl.Styles>

    <Style Selector="Button.chiaki-tab">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="{DynamicResource ThemeText3}"/>
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="FontWeight" Value="Medium"/>
      <Setter Property="Padding" Value="12,8"/>
      <Setter Property="MinHeight" Value="34"/>
      <Setter Property="BorderThickness" Value="0,0,0,2"/>
      <Setter Property="BorderBrush" Value="Transparent"/>
      <Setter Property="CornerRadius" Value="0"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>
    <Style Selector="Button.chiaki-tab.active">
      <Setter Property="Foreground" Value="#d4a853"/>
      <Setter Property="BorderBrush" Value="#d4a853"/>
    </Style>
    <Style Selector="Button.chiaki-tab:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="#08ffffff"/>
      <Setter Property="TextElement.Foreground" Value="{DynamicResource ThemeText2}"/>
    </Style>
    <Style Selector="Button.chiaki-tab:focus-visible /template/ ContentPresenter">
      <Setter Property="BoxShadow" Value="inset 0 0 0 1 #4dd4a853"/>
    </Style>

    <Style Selector="Button.ch-sm">
      <Setter Property="Background" Value="#14ffffff"/>
      <Setter Property="Foreground" Value="{DynamicResource ThemeText2}"/>
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="Padding" Value="10,8"/>
      <Setter Property="MinHeight" Value="34"/>
      <Setter Property="CornerRadius" Value="8"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="BorderBrush" Value="#10ffffff"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>
    <Style Selector="Button.ch-sm:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="#20ffffff"/>
      <Setter Property="TextElement.Foreground" Value="{DynamicResource ThemeText}"/>
    </Style>
    <Style Selector="Button.ch-sm:disabled /template/ ContentPresenter">
      <Setter Property="Opacity" Value="0.45"/>
    </Style>
    <Style Selector="Button.ch-sm:focus-visible /template/ ContentPresenter">
      <Setter Property="BoxShadow" Value="0 0 0 1 #4dd4a853"/>
    </Style>

    <Style Selector="Button.ch-primary">
      <Setter Property="Background" Value="#d4a853"/>
      <Setter Property="Foreground" Value="#07070d"/>
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Padding" Value="10,8"/>
      <Setter Property="MinHeight" Value="34"/>
      <Setter Property="CornerRadius" Value="8"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>
    <Style Selector="Button.ch-primary:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="#e8b85e"/>
    </Style>
    <Style Selector="Button.ch-primary:disabled /template/ ContentPresenter">
      <Setter Property="Opacity" Value="0.45"/>
    </Style>
    <Style Selector="Button.ch-primary:focus-visible /template/ ContentPresenter">
      <Setter Property="BoxShadow" Value="0 0 0 1 #4dd4a853"/>
    </Style>

    <Style Selector="Button.ch-danger">
      <Setter Property="Background" Value="#33ff4444"/>
      <Setter Property="Foreground" Value="#ff7070"/>
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="Padding" Value="8,8"/>
      <Setter Property="MinHeight" Value="34"/>
      <Setter Property="CornerRadius" Value="8"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>
    <Style Selector="Button.ch-danger:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="#55ff4444"/>
    </Style>
    <Style Selector="Button.ch-danger:disabled /template/ ContentPresenter">
      <Setter Property="Opacity" Value="0.45"/>
    </Style>
    <Style Selector="Button.ch-danger:focus-visible /template/ ContentPresenter">
      <Setter Property="BoxShadow" Value="0 0 0 1 #4dd4a853"/>
    </Style>

    <Style Selector="TextBox.ch-input">
      <Setter Property="Background" Value="#08ffffff"/>
      <Setter Property="Foreground" Value="{DynamicResource ThemeText}"/>
      <Setter Property="BorderBrush" Value="#14ffffff"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="8"/>
      <Setter Property="Padding" Value="10,8"/>
      <Setter Property="MinHeight" Value="34"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="CaretBrush" Value="#d4a853"/>
    </Style>
    <Style Selector="TextBox.ch-input:focus /template/ Border#PART_BorderElement">
      <Setter Property="BorderBrush" Value="#4dd4a853"/>
    </Style>

  </UserControl.Styles>

  <Border Background="{DynamicResource ThemeSurface}"
          BorderBrush="#14ffffff"
          BorderThickness="1,0,0,0">
  <Grid RowDefinitions="Auto,*">

    <!-- Header -->
    <Border Grid.Row="0" Padding="16,12,16,12"
            BorderBrush="#0affffff" BorderThickness="0,0,0,1">
      <Grid ColumnDefinitions="*,Auto">
        <StackPanel Grid.Column="0" Spacing="4" VerticalAlignment="Center">
          <TextBlock Text="PlayStation Remote Play" FontSize="14" FontWeight="SemiBold"
                     Foreground="{DynamicResource ThemeText}"/>
          <Border CornerRadius="4" Padding="6,2" Background="{Binding StatusBg}"
                  HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" Spacing="5">
              <Ellipse Width="5" Height="5" Fill="{Binding StatusDot}" VerticalAlignment="Center"/>
              <TextBlock Text="{Binding StatusText}" FontSize="10" FontWeight="SemiBold"
                         Foreground="{Binding StatusFg}"/>
              <TextBlock Text="{Binding StatusVersion}" FontSize="10" Foreground="{Binding StatusFg}"
                         Opacity="0.7"
                         IsVisible="{Binding StatusVersion, Converter={x:Static conv:ObjectConverters.IsNotNull}}"/>
            </StackPanel>
          </Border>
        </StackPanel>
        <Button Grid.Column="1" Classes="ch-sm" Content="Open chiaki-ng"
                Click="OpenGui_Click" VerticalAlignment="Center"/>
      </Grid>
    </Border>

    <!-- Content -->
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
      <StackPanel Margin="16,14,16,16" Spacing="14">

        <!-- ── Consoles ── -->
        <Grid ColumnDefinitions="*,Auto">
          <TextBlock Grid.Column="0" Text="MY CONSOLES" FontSize="9" FontWeight="Bold"
                     LetterSpacing="1.5" Foreground="#40b0aaa0" VerticalAlignment="Center"/>
          <Button Grid.Column="1" Classes="ch-sm" Content="+ Add"
                  Click="ShowAddConsole_Click"/>
        </Grid>

        <!-- Add form -->
        <Border IsVisible="{Binding ShowAddConsoleForm}"
                Background="#0fffffff" BorderBrush="#14ffffff" BorderThickness="1"
                CornerRadius="10" Padding="12">
          <StackPanel Spacing="8">
            <Grid ColumnDefinitions="*,12,*">
              <StackPanel Grid.Column="0" Spacing="4">
                <TextBlock Text="Nickname" FontSize="10" Foreground="#50b0aaa0"/>
                <TextBox Classes="ch-input" x:Name="NewNicknameBox" Watermark="PS5-Living-Room"/>
              </StackPanel>
              <StackPanel Grid.Column="2" Spacing="4">
                <TextBlock Text="Host IP" FontSize="10" Foreground="#50b0aaa0"/>
                <TextBox Classes="ch-input" x:Name="NewHostBox" Watermark="192.168.1.x"/>
              </StackPanel>
            </Grid>
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
              <Button Classes="ch-sm" Content="Cancel" Click="CancelAddConsole_Click"/>
              <Button Classes="ch-primary" Content="Add" Click="AddConsole_Click"/>
            </StackPanel>
          </StackPanel>
        </Border>

        <!-- Empty state -->
        <Border IsVisible="{Binding HasNoConsoles}"
                Background="#0affffff" CornerRadius="10" Padding="14">
          <StackPanel Spacing="6" HorizontalAlignment="Center">
            <TextBlock Text="No consoles yet" FontSize="12" FontWeight="SemiBold"
                       Foreground="{DynamicResource ThemeText2}" TextAlignment="Center"/>
            <TextBlock Text="Scan the network or add one manually."
                       FontSize="11" Foreground="#50b0aaa0" TextAlignment="Center"/>
          </StackPanel>
        </Border>

        <!-- Console cards -->
        <ItemsControl ItemsSource="{Binding Consoles}">
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="local:ChiakiConsoleViewModel">
              <Border Background="#0fffffff" BorderBrush="#14ffffff" BorderThickness="1"
                      CornerRadius="10" Padding="12,10" Margin="0,0,0,6">
                <Grid ColumnDefinitions="Auto,*,Auto">
                  <Border Grid.Column="0" Width="34" Height="34" CornerRadius="8"
                          Background="#1a3b82f6" Margin="0,0,10,0" VerticalAlignment="Center">
                    <TextBlock Text="PS" FontSize="10" FontWeight="Bold" Foreground="#3b82f6"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                  </Border>
                  <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                      <TextBlock Text="{Binding Nickname}" FontSize="12" FontWeight="SemiBold"
                                 Foreground="{DynamicResource ThemeText}"/>
                      <Border IsVisible="{Binding IsLive}" Background="#1a22c55e"
                              CornerRadius="4" Padding="5,1" VerticalAlignment="Center">
                        <TextBlock Text="LIVE" FontSize="9" FontWeight="Bold" Foreground="#22c55e"/>
                      </Border>
                    </StackPanel>
                    <TextBlock Text="{Binding Host}" FontSize="11" Foreground="#50b0aaa0"/>
                  </StackPanel>
                  <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                    <Button Classes="ch-danger" Content="Stop"
                            IsVisible="{Binding IsLive}"
                            Tag="{Binding Host}" Click="StopConsole_Click"/>
                    <Button Classes="ch-primary" Content="Connect"
                            IsVisible="{Binding !IsLive}"
                            Tag="{Binding .}" Click="ConnectConsole_Click"/>
                    <Button Classes="ch-sm" Content="Wake"
                            IsVisible="{Binding !IsLive}"
                            Tag="{Binding .}" Click="WakeConsole_Click"/>
                    <Button Classes="ch-sm" Tag="{Binding .}" Click="RemoveConsole_Click" Padding="8,6">
                      <PathIcon Width="9" Height="9"
                                Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                Data="M1,1 L9,9 M9,1 L1,9"/>
                    </Button>
                  </StackPanel>
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- ── Discover ── -->
        <Border Height="1" Background="#0affffff"/>

        <Grid ColumnDefinitions="*,Auto">
          <StackPanel Grid.Column="0" Spacing="2" VerticalAlignment="Center">
            <TextBlock Text="DISCOVER" FontSize="9" FontWeight="Bold"
                       LetterSpacing="1.5" Foreground="#40b0aaa0"/>
            <TextBlock Text="Find consoles on your local network."
                       FontSize="11" Foreground="{DynamicResource ThemeText3}"/>
          </StackPanel>
          <Button Grid.Column="1" Classes="ch-primary" Content="Scan" Click="Discover_Click"/>
        </Grid>

        <Border IsVisible="{Binding IsDiscovering}"
                Background="#0fd4a853" CornerRadius="6" Padding="10,7">
          <StackPanel Orientation="Horizontal" Spacing="8">
            <Ellipse Width="5" Height="5" Fill="#d4a853" VerticalAlignment="Center"/>
            <TextBlock Text="Scanning…" FontSize="11" FontWeight="SemiBold" Foreground="#d4a853"/>
          </StackPanel>
        </Border>

        <ItemsControl ItemsSource="{Binding DiscoveredConsoles}">
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="local:DiscoveredConsoleViewModel">
              <Border Background="#0fffffff" BorderBrush="#14ffffff" BorderThickness="1"
                      CornerRadius="10" Padding="12,10" Margin="0,0,0,6">
                <Grid ColumnDefinitions="Auto,*,Auto">
                  <Border Grid.Column="0" Width="34" Height="34" CornerRadius="8"
                          Background="#1a3b82f6" Margin="0,0,10,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding TypeLabel}" FontSize="9" FontWeight="Bold"
                               Foreground="#3b82f6"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                  </Border>
                  <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource ThemeText}"/>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                      <TextBlock Text="{Binding Host}" FontSize="11" Foreground="#50b0aaa0"/>
                      <Border IsVisible="{Binding IsReady}" Background="#1a22c55e"
                              CornerRadius="3" Padding="4,1">
                        <TextBlock Text="Awake" FontSize="9" Foreground="#22c55e"/>
                      </Border>
                      <Border IsVisible="{Binding IsStandby}" Background="#14ffffff"
                              CornerRadius="3" Padding="4,1">
                        <TextBlock Text="Standby" FontSize="9" Foreground="{DynamicResource ThemeText3}"/>
                      </Border>
                    </StackPanel>
                  </StackPanel>
                  <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                    <Button Classes="ch-primary" Content="Add"
                            IsVisible="{Binding !AlreadyAdded}"
                            Tag="{Binding .}" Click="AddDiscovered_Click"/>
                    <Button Classes="ch-sm" Content="Added"
                            IsVisible="{Binding AlreadyAdded}"
                            IsEnabled="False"/>
                  </StackPanel>
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- ── Pair ── -->
        <Border Height="1" Background="#0affffff"/>

        <TextBlock Text="PAIR A CONSOLE" FontSize="9" FontWeight="Bold"
                   LetterSpacing="1.5" Foreground="#40b0aaa0"/>

        <StackPanel Spacing="8">
          <StackPanel Spacing="4">
            <TextBlock Text="Console IP" FontSize="10" Foreground="#50b0aaa0"/>
            <TextBox Classes="ch-input" x:Name="RegHostBox" Watermark="192.168.1.x"/>
          </StackPanel>
          <StackPanel Spacing="4">
            <TextBlock Text="PSN Account ID (Base64)" FontSize="10" Foreground="#50b0aaa0"/>
            <TextBox Classes="ch-input" x:Name="RegAccountIdBox" Watermark="ab12CDef3ghIjk…"/>
          </StackPanel>
          <StackPanel Spacing="4">
            <TextBlock Text="Link Code (8 digits)" FontSize="10" Foreground="#50b0aaa0"/>
            <TextBox Classes="ch-input" x:Name="RegPinBox" Watermark="12345678" MaxLength="8"/>
          </StackPanel>
        </StackPanel>

        <Border IsVisible="{Binding RegStatus, Converter={x:Static conv:ObjectConverters.IsNotNull}}"
                CornerRadius="6" Padding="10,8" Background="{Binding RegStatusBg}">
          <TextBlock Text="{Binding RegStatus}" FontSize="11"
                     Foreground="{Binding RegStatusFg}" TextWrapping="Wrap"/>
        </Border>

        <StackPanel Orientation="Horizontal" Spacing="8">
          <Button Classes="ch-primary" Content="Pair" Click="Register_Click"/>
          <Button Classes="ch-sm" Content="Reset" Click="RegReset_Click"/>
        </StackPanel>

      </StackPanel>
    </ScrollViewer>

  </Grid>
  </Border>
</UserControl>
